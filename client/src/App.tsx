/* CSS & UI */
import './App.css';
import FileUploadForm from './components';

const App: React.FC = (): JSX.Element => {
  return (
    <div className='App'>
      <FileUploadForm />
    </div>
  );
};

export default App;
